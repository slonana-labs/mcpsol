[package]
name = "mcpsol-native"
version = "0.1.0"
edition = "2021"
description = "MCP schema generation for native Solana programs"
license = "MIT OR Apache-2.0"

[features]
default = []

[dependencies]
mcpsol-core = { path = "../core" }
solana-program.workspace = true

[lints]
workspace = true
